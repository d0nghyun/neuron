---
name: "{{name}}"
description: "{{description}}"
tools: "{{tools}}"
model: "{{model}}"
---

# {{name}} Task Agent

> Task-oriented agent for specific operations. Generated by Neuron Factory.

## Input Specification

```yaml
input:
  required:
    {{#input_required}}
    - name: "{{name}}"
      type: "{{type}}"
      description: "{{description}}"
    {{/input_required}}
  optional:
    {{#input_optional}}
    - name: "{{name}}"
      type: "{{type}}"
      default: "{{default}}"
    {{/input_optional}}
```

## Output Specification

```yaml
output:
  format: "{{output_format}}"
  fields:
    {{#output_fields}}
    - name: "{{name}}"
      type: "{{type}}"
      description: "{{description}}"
    {{/output_fields}}
```

## Success Criteria

> P15 Verify Before Done: Prove it works, don't assume.

| Criterion | Validation |
|-----------|------------|
{{#success_criteria}}
| {{criterion}} | {{validation}} |
{{/success_criteria}}

## Execution Steps

### Step 1: Validate Input

```yaml
# Check required inputs
{{#input_required}}
assert {{name}} is not null
{{/input_required}}
```

### Step 2: Execute Task

{{task_execution}}

### Step 3: Validate Output

```yaml
# Check output against success criteria
{{#success_criteria}}
assert {{criterion}}
{{/success_criteria}}
```

### Step 4: Return Result

```yaml
{{name}}_result:
  status: success | failed
  {{#output_fields}}
  {{name}}: <value>
  {{/output_fields}}
  metadata:
    duration_ms: <execution time>
    timestamp: <ISO 8601>
```

## Error Handling

| Error Type | Condition | Action |
|------------|-----------|--------|
{{#error_handling}}
| {{type}} | {{condition}} | {{action}} |
{{/error_handling}}

## Guardrails

- **NEVER** proceed with invalid input
- **ALWAYS** validate output before returning
- **ALWAYS** include error details on failure

## Principle Checklist

- [ ] P7 Test-First: Success criteria defined before execution
- [ ] P9 Root Cause: Errors traced to source
- [ ] P15 Verify: Output validated against criteria

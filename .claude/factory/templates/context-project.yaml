# Project Context Template
# Generated by Neuron Factory
# Path: .claude/contexts/ctx-{{module}}.yaml

name: "ctx-{{module}}"
description: "{{description}}"
module: "{{module}}"

# ═══════════════════════════════════════════════════════════════════════════════
# VARIABLES - Key-value pairs injected into agent prompts
# ═══════════════════════════════════════════════════════════════════════════════

variables:
  # API Configuration
  api_base_url: "{{api_base_url}}"
  api_version: "{{api_version}}"

  # Project Identifiers
  project_id: "{{project_id}}"
  project_key: "{{project_key}}"

  # Custom Variables
  # {{#custom_variables}}
  # {{key}}: "{{value}}"
  # {{/custom_variables}}

# ═══════════════════════════════════════════════════════════════════════════════
# INSTRUCTIONS - Module-specific guidance for agents
# ═══════════════════════════════════════════════════════════════════════════════

instructions:
  # Critical facts agents must know
  must_know:
    # - "{{module}} uses X pattern for Y"
    # - "API requires header Z"

  # Actions to avoid
  must_avoid:
    # - "Never call endpoint X without Y"
    # - "Don't assume Z"

  # Recommended patterns
  should_follow:
    # - "Always validate before submit"
    # - "Use pagination for lists > 100"

# ═══════════════════════════════════════════════════════════════════════════════
# DEPENDENCIES - Other contexts/skills this context requires
# ═══════════════════════════════════════════════════════════════════════════════

dependencies:
  contexts: []
    # - ctx-parent-module

  skills: []
    # - api-github
    # - api-jira

  agents: []
    # - advisor

# ═══════════════════════════════════════════════════════════════════════════════
# VALIDATION - Rules for context consistency
# ═══════════════════════════════════════════════════════════════════════════════

validation:
  # Required variables
  required_variables:
    - api_base_url

  # Variable format patterns
  patterns:
    api_base_url: "^https?://"
    api_version: "^v[0-9]+"

# ═══════════════════════════════════════════════════════════════════════════════
# METADATA
# ═══════════════════════════════════════════════════════════════════════════════

_meta:
  created: "{{date}}"
  created_by: factory
  template: context-project
  version: 1

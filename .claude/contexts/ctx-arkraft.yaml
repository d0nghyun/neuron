# Arkraft Project Context
# Generated by Neuron Factory
# Path: .claude/contexts/ctx-arkraft.yaml

name: ctx-arkraft
description: "Arkraft demo delivery project context"
module: arkraft

# ═══════════════════════════════════════════════════════════════════════════════
# VARIABLES - Key-value pairs injected into agent prompts
# ═══════════════════════════════════════════════════════════════════════════════

variables:
  # API Configuration
  api_base_url: "https://api.arkraft.io"
  api_version: "v1"

  # Project Identifiers
  jira_board: "ARK"
  jira_project_key: "ARK"

# ═══════════════════════════════════════════════════════════════════════════════
# INSTRUCTIONS - Module-specific guidance for agents
# ═══════════════════════════════════════════════════════════════════════════════

instructions:
  # Critical facts agents must know
  must_know:
    - "arkraft Jira board is ARK (not ARKRAFT)"
    - "arkraft API base URL is api.arkraft.io"
    - "arkraft uses Python 3.11+ with uv package manager"

  # Actions to avoid
  must_avoid:
    - "Never call /deploy endpoint without explicit user confirmation"
    - "On import error, check __init__.py first (90% of cases)"

  # Recommended patterns
  should_follow:
    - "Run lint && test before any deploy"
    - "Use pagination for API lists > 100 items"

# ═══════════════════════════════════════════════════════════════════════════════
# DEPENDENCIES - Other contexts/skills this context requires
# ═══════════════════════════════════════════════════════════════════════════════

dependencies:
  contexts: []

  skills:
    - api-jira

  agents:
    - advisor

# ═══════════════════════════════════════════════════════════════════════════════
# METADATA
# ═══════════════════════════════════════════════════════════════════════════════

_meta:
  created: "2026-01-25"
  created_by: factory
  template: context-project
  version: 1
